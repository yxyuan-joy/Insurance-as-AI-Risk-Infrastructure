extends: ../formal.yaml

# Sensitivity: denser peer-information network.
# Tests whether the mechanism remains stable when local social evidence is more available.
network:
  same_industry_neighbors: 16
  cross_industry_neighbors: 5
