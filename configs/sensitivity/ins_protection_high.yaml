extends: ../formal.yaml

# Sensitivity: more generous insurance protection.
# Tests whether stronger coverage amplifies the same risk-transfer mechanism.
insurance_pricing:
  coverage_ratio_delta: 0.20
  deductible_ratio_delta: -0.14
