extends: ../formal.yaml

# Sensitivity: higher realized AI operational loss.
# Tests whether insurance still improves aggregate outcomes when AI tail exposure is stronger.
simulation:
  action_loss_scale: 3.70
